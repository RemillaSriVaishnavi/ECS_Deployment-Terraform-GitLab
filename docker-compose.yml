version: "3.9"

services:

  ecs-demo-app:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: ecs-demo-app

    ports:
      - "8080:80"

    environment:
      PORT: 80

    restart: unless-stopped